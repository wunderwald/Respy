// bioGame sound configuration — volumes only.
// File paths come from the active scene definition (bioGame_scene.js).
// Volumes are linear 0–1.

export const SOUND_CONFIG = {
  // ── Levels ───────────────────────────────────────────────────────────────
  AMBIENCE_VOLUME:  0.5,
  NOISE_VOLUME_MIN: 0.0,
  NOISE_VOLUME_MAX: 0.8,
  COLLECT_VOLUME:   0.7,

  // ── Timing ───────────────────────────────────────────────────────────────
  FADE_SECS: 1.0,
};
